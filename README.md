# tiahold.com

```bash
git clone ...
cd tiahold.com
virtualenv ENV
. ENV/bin/activate
pip install -r requirements.txt
```
get the account.key for let's encrypt
get the aws credentials

```bash
aws configure
zappa deploy
```
